name: WIP Test / E2E

on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - feature/github-actions-migration-WPEX-2662

jobs:
  chrome_e2e:
    name: Chrome
    uses: ./.github/workflows/test-e2e-cypress.yml
