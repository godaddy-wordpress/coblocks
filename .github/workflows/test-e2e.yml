name: WIP Test / E2E

on:
  pull_request:
    branches:
      - master

jobs:
  chrome_e2e:
    name: Chrome
    uses: ./.github/workflows/test-e2e-cypress.yml

  chrome_e2e_wp_60:
    name: "Chrome - WP 6"
    uses: ./.github/workflows/test-e2e-cypress.yml
    with:
      wpVersion: "WordPress/WordPress#6.0.3"

  # firefox_e2e:
  #   name: Firefox
  #   uses: ./.github/workflows/test-e2e-cypress.yml
  #   with:
  #     browser: firefox
