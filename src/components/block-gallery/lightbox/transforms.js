/**
 * Set the attributes for the component transformations
 * @type {Object}
 */
function LightboxTransforms( props ) {

	const transforms = {
		lightbox: props.lightbox,
		lightboxStyle: props.lightboxStyle,
	};

	return transforms;
}

export default LightboxTransforms;