/**
 * WordPress dependencies
 */
import { Icon } from '@wordpress/components';

/**
 * External dependencies
 */
import { AtIcon } from '@godaddy-wordpress/coblocks-icons';

/**
 * Block icons
 */
const icons = {};

icons.at = <Icon icon={ AtIcon } />;

export default icons;
